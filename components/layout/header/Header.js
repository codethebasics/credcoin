import styles from '../../../styles/components/Header.module.scss';

export default function Header(props) {
    return (
        <div id={styles.headerContainer}>
            <div>Voltar como administrador</div>
            <div>x</div>
        </div>
    );
}
